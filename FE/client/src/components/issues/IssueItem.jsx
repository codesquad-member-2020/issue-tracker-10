import React from "react";
import { TableItem } from "@style/CustomStyle";
import styled from "styled-components";
import moment from "moment";

const IssueItem = ({ issue }) => {
  return (
    <TableItem>
      <div>Issue Item Layout</div>
    </TableItem>
  );
};

export default IssueItem;
